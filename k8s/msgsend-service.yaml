apiVersion: v1
kind: Service
metadata:
  name: msgsend-service
  namespace: producao
  labels:
    app: msgsend
spec:
  selector:
    app: msgsend
  ports:
    - name: http
      protocol: TCP
      port: 8088
      targetPort: 8088
      nodePort: 31005
  type: NodePort
